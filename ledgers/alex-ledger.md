# Alex's published-work ledger

Follow [the ledger rules](../AGENTS.md#personal-ledgers--update-with-the-work-you-push). Append only Alex's work; current task status belongs in GitHub Issues.

2026-09-12T05:52Z → [#4](https://github.com/ethancurb/cmu-hackathon/issues/4) → added `CONTEXT.md` (domain glossary), `docs/adr/0001-mongodb-for-mock-timeseries.md`, `docs/adr/0002-commute-agent-as-differentiator.md`; added pointer notes into `docs/PROJECT.md` and `docs/ARCHITECTURE.md` superseding the Postgres option → verification: `git diff --check` clean, ADR filenames match `NNNN-slug.md`, no code changed → next: open T1–T6 implementation tickets referencing these ADRs; no implementation yet.

2026-09-12T08:15Z → [#6](https://github.com/ethancurb/cmu-hackathon/issues/6) → pivoted the demo path off MongoDB after Atlas proved unreachable from CMU network (`128.2.149.231`; every port to the cluster nodes silently dropped while Atlas's own web endpoint answered — IP access list, not diagnosed further). Added `lib/sources/mock-memory.ts` (`MockMemorySource`, reads `data/fixture.ts` in process — no database, no file read, no network) and made it the `DATA_MODE=mock` default; `MockMongoSource` moved behind a new `DATA_MODE=mongo` and is retained unchanged for the ADR-0001 time-series story. Extracted every mapping decision into `lib/sources/readings.ts` so the two sources cannot drift. Added `stop(stopId)` to the `CapacitySource` interface (T3 needs `routesServing` to emit shell-route cards without reaching past the abstraction); implemented on all three sources. → verification: `npm run typecheck` clean; `npm run smoke:source` executed for the first time — **30/30 checks pass** against `MockMemorySource`, covering factory selection and default, unknown/hybrid DATA_MODE throwing, the PRT stub returning empty, stop lookup incl. shell route `71B` in routesServing, three ordered 71D arrivals at Morewood, current-vs-forecast split (9001: current 14/60 @7101 vs forecast 56/60; at +10min current 55/60 @7104), un-started runs returning `{kind:"unknown"}` / `source:"none"` with no passenger count, simNow filtering, empty results for unknown stop/run and past-the-last-bus, and runFuture 31→46→55→56 ending full. → T2 (#6) is now genuinely verified and can be closed once integrated. T1 (#5) code is written and typechecks but its seed has still never run — Atlas unreachable — and it is no longer on the demo path; #5 should be rescoped or deferred. Also added `data/export-json.ts` + `npm run export:json`, generating `data/fixture.json` (portable plain-JSON copy of the same data, ISO timestamps) for handoff to anyone not importing the TypeScript. `fixture.ts` remains the source of truth; the JSON is generated.
