# Nate's published-work ledger

Follow [the ledger rules](../AGENTS.md#personal-ledgers--update-with-the-work-you-push). Append only Nate's work; current task status belongs in GitHub Issues.

2026-09-12T08:10:41Z → issue #15 → added real weather (Open-Meteo, key-free) and a real map basemap (MapLibre GL + Esri World Light Gray Base raster tiles, key-free) to the home map module, replacing the hardcoded "Ends in 18m" chip and the hand-drawn SVG grid; centered on browser geolocation with a CMU/Oakland fallback; recorded the rendering choice in docs/ARCHITECTURE.md (issue #8's open question) → verified: `npm run build` and `npm run lint` clean; Playwright screenshots against the dev server (geolocation granted and denied) show real CMU-area streets and a live weather reading, no console/page errors → risk: not yet tested on a real device/browser outside this sandbox; MapLibre's vector-tile pipeline failed silently in this sandbox specifically, so raster was chosen over vector — worth rechecking on the actual demo machine if per-layer recoloring is wanted later.
2026-09-12T08:27:03Z → issue #15 → replaced the map's three fake bezier route paths/badges with real PRT route geometry (public/route-shapes.geojson, extracted from PRT's key-free published GTFS static feed), hand-projected onto the map via map.project() (avoids the same worker pipeline that silently failed for vector tiles/native GeoJSON sources in this sandbox); dropped the old "current bus position" lime marker rather than carry a fake vehicle position onto a now-accurate map → verified: build/lint clean; Playwright screenshots confirm all three routes render near CMU and switch active/inactive styling correctly on route selection → risk/open question: mock route labels "71"/"61" have no bare-ID match in PRT's real system (only lettered branches do) — substituted 71→71D and kept 61→61A based on measured distance from CMU, documented in docs/ARCHITECTURE.md, but not yet confirmed with the team as the intended branches.
2026-09-12T08:41:02Z → issue #15 → made the home LocationField a live address autocomplete (lib/geocode.ts, Photon — key-free, built for typeahead), and wired a picked address's real coordinate through page.tsx → RouteMap → MapCanvas to fit the map to origin+destination and auto-select whichever of the 3 tracked routes passes nearest (nearestRouteId, flat-plane vertex distance) → verified: build/lint clean; Playwright end-to-end test typed real addresses and confirmed two different ones correctly selected two different routes (Squirrel Hill → 61, North Shore → 54), map visibly re-fit both times, no console errors → fixed along the way: (1) MapLibre event handlers registered once at mount were reading stale lat/lng/destination via closure on later moveend events — moved to a ref updated every render; (2) Esri's World_Light_Gray_Base silently 200s a placeholder "Map data not yet available" tile past z16 instead of erroring — fitBounds's post-zoom was landing past that, fixed by declaring the source's real maxzoom so MapLibre overzooms instead of requesting it. Both documented in docs/ARCHITECTURE.md.
2026-09-12T08:56:32Z → issue #15 → replaced mock arrival times ("8:46"/"On time") across ArrivalCards, RouteListView, and the home summary with live PRT GTFS-realtime TripUpdate predictions (new app/api/arrival-times/route.ts, server-side since that feed has no CORS headers, in-memory-cached 15s; lib/prt-trip-updates.ts parses PRT's debug-text feed format same as scripts/probe-prt.mjs does for VehiclePositions; lib/prt-routes.ts holds ~700m-of-CMU real stop_ids per route, extracted once from PRT's GTFS static feed); deliberately named /api/arrival-times, not /api/arrivals, to avoid colliding with issue #7/T3's planned CapacitySource-backed contract — this endpoint never reports a seat count, only a time; also cached the searched address to localStorage (lib/destination-cache.ts) so it survives a reload until a new one is picked → verified: build/lint clean; hit the live API directly and got real per-route predictions (or an honest {"status":"unavailable"} when PRT's feed had no active trip for that route, which was common off-peak) with correct stop names and timezone-correct clock times; Playwright confirmed the cached address and its auto-selected route both survive a page reload, no console errors → risk/open question: no static-schedule fallback for when live is unavailable (would need calendar.txt-aware service-day filtering, cut for time) — routes will legitimately show "—" whenever PRT's feed has nothing in-progress for them, which was frequent in testing; removed the now-dead Route.scheduledTime/arrivesIn/status mock fields from lib/mock-data.ts since nothing read them anymore.

2026-09-12T09:10:00Z ? https://github.com/ethancurb/cmu-hackathon/issues/18 ? fit home and Plan to the mobile dynamic viewport with safe-area padding, flexible map/list panel, compact short-phone spacing, bounded arrival cards, and no document overscroll; hide duplicate arrival cards in mobile list mode ? verification: npm run lint and npm run build passed; git diff --check passed; Playwright map/list/Plan at 320x568, 375x667, 390x844, 430x932 passed document/shell overflow, footer visibility, and attempted page-scroll checks; visually inspected 320px screenshots ? limits: Chromium emulation only; physical iOS keyboard, safe-area behavior, and landscape not verified; branch publication does not mean main integration.
2026-09-12T11:20:00Z → https://github.com/ethancurb/cmu-hackathon/issues/19 → Transit Pressure v2 shipped end to end: `lib/pressure` (config-centralized interpretable engine: hourly temporal profile, event magnitude×corridor-proximity×time curves with estimated-end widening, weather, effect-weighted PRT alerts, decaying realtime evidence, service gaps, HIGH/MEDIUM/LOW confidence, surge/best windows, structured recommendation, event impacts), live adapters (MLB/NHL/ESPN schedules, Open-Meteo hourly, PRT GTFS-RT protobuf, GTFS snapshot), `/api/pressure` LIVE+DEMO, `usePressure` hook, home PressureModule (dots/score/level/confidence/surge/WHY/advice/event impact/sources), real pressure timeline + departure windows on /plan, `/demo` presenter with staged scenarios and `?demo=` presenter strip on home; removed mock seats/walk-time/BARS occupancy claims; mobile scroll restored and desktop renders a centered phone column → verification: `npm test` 16/16 pass (pirates 31→62→73→91, concert 25→52→63→81, cmu 32→43→61), `npm run typecheck`/`lint`/`build` clean, foreground `npm start -- --port 3100` + `node scripts/qa-screens.mjs` (Playwright 320/390/768/1280) no overflow, no console errors, no occupancy text; live API at CMU returned all sports feeds, weather and PRT realtime LIVE → risks: Ticketmaster concerts need optional key; event ends are duration assumptions; weights are documented heuristics; not yet tested on a physical phone.
2026-09-12T11:55:00Z → https://github.com/ethancurb/cmu-hackathon/issues/19 → future-anchored timelines now advise "Leave at <time>" instead of "Leave now"; event venue marker on the map for the major contributor; README (root + frontend), docs/overnight-report.md (model, sources, assumptions, demo script), PROJECT decision, ARCHITECTURE pressure contract/endpoint, DEMO three-minute script, overnight plan checkboxes, page metadata no longer mentions "how full" → verification: `npm test` 16/16, typecheck/lint clean, `npm run build` clean, foreground server + `node scripts/qa-screens.mjs` (no overflow/console errors) and `node scripts/qa-flow.mjs` (journey, demo stepping 31→91, Tomorrow/8 h plan shows "Leave at 7:00 AM") → risks: docs describe heuristics, not fitted weights; physical-phone check still pending.
2026-09-12T12:20:00Z → https://github.com/ethancurb/cmu-hackathon/issues/19 → live hamburger menu (Home / Pressure timeline / Scenarios) replaces the dead control; map stats button opens /plan; new scripts/qa-fail.mjs (503 + aborted API show honest unavailable states) and scripts/qa-menu.mjs; report/README updated → verification: typecheck/lint/build clean, `npm test` 16/16, fresh foreground server + qa-screens (no overflow/console errors), qa-flow, qa-fail, qa-menu all pass; live Steelers-Sunday corridor check shows the real ESPN game raising tomorrow's timeline to 67 → risks: physical phone unverified; live mode today has no Pittsburgh home game, so the staged scenarios carry the event story.
2026-09-12T12:45:00Z → https://github.com/ethancurb/cmu-hackathon/issues/19 → `PressureResult.upcoming`: in-reach events beyond the timeline (next 48 h) with arrivals-peak and exit-wave times, shown on the home module as "Upcoming on this trip" when no event is impacting the current window; test added → verification: `npm test` 17/17, typecheck/lint/build clean, fresh foreground server: live CMU → North Shore query lists the real Steelers game (exit wave ~4:30 PM Sun, 0.6 km); qa-screens/qa-flow/qa-fail/qa-menu all pass → risks: unchanged (heuristic weights, no physical-phone check).
2026-09-12T17:45:00Z → https://github.com/ethancurb/cmu-hackathon/issues/20 → map-centered planner on the home screen: shared trip state (`lib/app-context`: origin/destination/time/arrive-by/prefs/selected journey/rider signals/chat); provider-backed itineraries via Transitous (MOTIS over PRT GTFS, key-free, verified) in `lib/journey` + `/api/journey` with validated legs, walking/waiting/riding breakdown, realtime-vs-scheduled labels and latest-request-wins polling; interactive MapLibre map (pan, cooperative zoom, +/−, Fit route) drawing walk/board/alight/transfer legs; concise home with collapsed Advice/WHY/Sources; chat planner sheet replacing the public Scenarios page (`/api/chat`: Claude tool loop when `ANTHROPIC_API_KEY` is set, deterministic guided parser otherwise, both through validated actions; ambiguity questions, follow-ups, rider reports); pressure additions (event categories, VERIFIED/RIDER evidence, per-sample reason detail, `events`, `coverageGaps`, POST rider signals, Ticketmaster venue coordinates); `/plan` bars keyboard-selectable with a "What's happening today/tomorrow" evidence section; `@anthropic-ai/sdk` added; docs (PROJECT, ARCHITECTURE, README, DEMO) → verification: `npm test` 36/36 (pressure, adapters, journey, chat, explain), typecheck/lint/build clean, fresh foreground `npm start -- --port 3100` + qa-screens (320/390/768/1280 no overflow/console errors), qa-flow, qa-fail, qa-menu (/demo 404), qa-journey (chat → 3 real journey cards → museum clarification → Warhol on map; panel ETA = button ETA before/after journey switch and Leave now; drag/zoom/fit; bar click opens evidence, ← → moves selection; routing 503, chat 503 and denied geolocation shown honestly) all pass; live `/api/journey` CMU→North Shore returned 61A + SLVR with realtime PRT legs → risks: no `ANTHROPIC_API_KEY` in this environment so the Claude path is typechecked but unexercised (guided mode is what was verified); Ticketmaster still needs its key; Transitous is a public best-effort instance; scenarios run with itinerary search off; physical-phone check pending.
2026-09-12T18:40:00Z → https://github.com/ethancurb/cmu-hackathon/issues/20 → keys wired (`busappfrontend/.env.local`, git-ignored; root `.env` also ignored); `plan_trip` tool now resolves place labels server-side so a Claude turn needs one round trip instead of three; Ticketmaster junk listings (venue tours, hotel packages, VIP add-ons) filtered with a test → verification: `npm test` 37/37, typecheck/lint/build clean; live: `/api/chat` reports mode model, one real Claude turn ("CMU to the North Shore by 7") returned validated actions + three real journeys with pressure advice in 37 s before the account's credit ran out, after which `/api/chat` fell back to the guided parser with the `fallback` notice as designed; Ticketmaster LIVE with real Pittsburgh concerts/theater/sports (Lion King at the Benedum, Riverhounds, Stage AE shows); qa-journey passes on the rebuilt server → risks: Anthropic account needs credit for the model path (the tuned single-call flow is untested live); public Transitous instance; physical-phone check pending.
