# Nate's published-work ledger

Follow [the ledger rules](../AGENTS.md#personal-ledgers--update-with-the-work-you-push). Append only Nate's work; current task status belongs in GitHub Issues.

2026-09-12T08:10:41Z → issue #15 → added real weather (Open-Meteo, key-free) and a real map basemap (MapLibre GL + Esri World Light Gray Base raster tiles, key-free) to the home map module, replacing the hardcoded "Ends in 18m" chip and the hand-drawn SVG grid; centered on browser geolocation with a CMU/Oakland fallback; recorded the rendering choice in docs/ARCHITECTURE.md (issue #8's open question) → verified: `npm run build` and `npm run lint` clean; Playwright screenshots against the dev server (geolocation granted and denied) show real CMU-area streets and a live weather reading, no console/page errors → risk: not yet tested on a real device/browser outside this sandbox; MapLibre's vector-tile pipeline failed silently in this sandbox specifically, so raster was chosen over vector — worth rechecking on the actual demo machine if per-layer recoloring is wanted later.
2026-09-12T08:27:03Z → issue #15 → replaced the map's three fake bezier route paths/badges with real PRT route geometry (public/route-shapes.geojson, extracted from PRT's key-free published GTFS static feed), hand-projected onto the map via map.project() (avoids the same worker pipeline that silently failed for vector tiles/native GeoJSON sources in this sandbox); dropped the old "current bus position" lime marker rather than carry a fake vehicle position onto a now-accurate map → verified: build/lint clean; Playwright screenshots confirm all three routes render near CMU and switch active/inactive styling correctly on route selection → risk/open question: mock route labels "71"/"61" have no bare-ID match in PRT's real system (only lettered branches do) — substituted 71→71D and kept 61→61A based on measured distance from CMU, documented in docs/ARCHITECTURE.md, but not yet confirmed with the team as the intended branches.
2026-09-12T08:41:02Z → issue #15 → made the home LocationField a live address autocomplete (lib/geocode.ts, Photon — key-free, built for typeahead), and wired a picked address's real coordinate through page.tsx → RouteMap → MapCanvas to fit the map to origin+destination and auto-select whichever of the 3 tracked routes passes nearest (nearestRouteId, flat-plane vertex distance) → verified: build/lint clean; Playwright end-to-end test typed real addresses and confirmed two different ones correctly selected two different routes (Squirrel Hill → 61, North Shore → 54), map visibly re-fit both times, no console errors → fixed along the way: (1) MapLibre event handlers registered once at mount were reading stale lat/lng/destination via closure on later moveend events — moved to a ref updated every render; (2) Esri's World_Light_Gray_Base silently 200s a placeholder "Map data not yet available" tile past z16 instead of erroring — fitBounds's post-zoom was landing past that, fixed by declaring the source's real maxzoom so MapLibre overzooms instead of requesting it. Both documented in docs/ARCHITECTURE.md.
