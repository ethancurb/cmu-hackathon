# XD60 visualization assets

Imported for issue #22 from the user-supplied `xd60-model-package.zip` on 2026-09-12.

- `new-flyer-xd60.glb` is the runtime GLB 2.0 asset. SHA-256: `01ecddedeb27ff4eb963276ee223195d707cdf0e6c20579c7c9e2f73420a2b70`.
- `xd60-exterior.png` and `xd60-cutaway.png` are loading/error fallbacks.
- `xd60-model-definition.json` records the supplied dimensions and accuracy statement.
- `THREE-LICENSE.txt` is the license delivered with the package for Three.js. The app's renderer is installed separately through npm.

The GLB is a stylized, passenger-free concept. It is not a verified PRT fleet replica, engineering model, occupancy source, or passenger-capacity claim. Its 47 seat meshes describe the illustration only.
